<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://khalilbalaree.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://khalilbalaree.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-06-16T22:47:24+00:00</updated><id>https://khalilbalaree.github.io/feed.xml</id><title type="html">blank</title><subtitle>I am Zijun Wu, a PhD student at the University of Alberta. My research interests include natural language processing and machine learning.</subtitle><entry><title type="html">TokMem: packing a whole procedure into a single token</title><link href="https://khalilbalaree.github.io/blog/2026/tokmem/" rel="alternate" type="text/html" title="TokMem: packing a whole procedure into a single token"/><published>2026-06-16T00:00:00+00:00</published><updated>2026-06-16T00:00:00+00:00</updated><id>https://khalilbalaree.github.io/blog/2026/tokmem</id><content type="html" xml:base="https://khalilbalaree.github.io/blog/2026/tokmem/"><![CDATA[<p>If you use a language model for anything repetitive, you have probably built up a little collection of prompts. The careful instructions for formatting a meeting summary. The system message that makes the model answer like a patient tutor. The five-paragraph specification that turns it into a halfway-decent SQL generator.</p> <p>Each of those is a <em>procedure</em> — a reusable recipe for getting the model to behave a certain way. And every single time you want to use one, you have to paste the whole thing back into the context window. The model then re-reads it, token by token, before it does anything useful. The recipe never gets <em>learned</em>; it gets <em>re-explained</em>, over and over.</p> <p>That re-explaining is the problem <a href="/publications/">TokMem</a> <a class="citation" href="#wu2025tokmem">(Wu et al., 2026)</a> sets out to fix.</p> <h2 id="the-cost-of-explaining-yourself-every-time">The cost of explaining yourself every time</h2> <p>Prompting is a wonderfully flexible way to steer a model, but it has two stubborn downsides.</p> <p>First, <strong>it doesn’t scale with reuse.</strong> A long procedure costs the same amount of compute on the thousandth query as it did on the first, because the model reprocesses the entire prompt afresh each time. The recipe is stateless.</p> <p>Second, <strong>prompts don’t compose cleanly.</strong> If you have twenty procedures and a task needs three of them, you end up concatenating three blocks of instructions and hoping they don’t step on each other. There’s no tidy way to say “use procedure #7 here, then procedure #12.”</p> <div class="text-center"> <img src="/assets/img/blog/fig1-prompt-vs-tokmem.svg" alt="On the left, a long prompt is re-sent to the model on every query; on the right, TokMem feeds a single memory token to a frozen model." style="max-width:100%; height:auto;"/> <p class="post-description">Prompting re-feeds the whole recipe each time; TokMem replaces it with one small token.</p> </div> <h2 id="one-token-per-procedure">One token per procedure</h2> <p>TokMem’s idea is to stop re-explaining and start <em>remembering</em>. It compiles each reusable procedure into a <strong>single trainable token</strong> — a “memory token” — and stores it away. When you want that behavior, you don’t paste a paragraph; you just reference the token.</p> <p>The neat part is that this one token plays two roles at once:</p> <ul> <li>It’s an <strong>index</strong> — it identifies <em>which</em> procedure you’re invoking, the way a name in a phone book points to a specific entry.</li> <li>It’s a <strong>control signal</strong> — once selected, it actively <em>steers</em> the model’s generation toward that behavior.</li> </ul> <p>So a single unit answers both “which recipe?” and “now cook it.” And because it’s always just one token, the overhead is <strong>constant</strong> — it doesn’t grow with how long or complicated the original procedure was.</p> <div class="text-center"> <img src="/assets/img/blog/fig2-memory-token.svg" alt="A single memory token acts as both a procedure index and a generation control signal, feeding a frozen language model." style="max-width:100%; height:auto;"/> <p class="post-description">One memory token, two jobs: pick the procedure, then steer the generation.</p> </div> <h2 id="keeping-the-model-frozen">Keeping the model frozen</h2> <p>Here’s the design choice I find most satisfying. The backbone language model itself <strong>never changes.</strong> All of the procedural knowledge lives inside these small, dedicated memory tokens, not in the model’s weights.</p> <p>That separation buys two things that are hard to get otherwise:</p> <ul> <li><strong>You can keep adding procedures forever.</strong> Learning a new memory token doesn’t touch the existing ones, so there’s no catastrophic forgetting — adding skill #500 doesn’t quietly break skill #3.</li> <li><strong>No retraining the whole model.</strong> Each procedure is a tiny, isolated thing you train on its own, while the expensive backbone stays put.</li> </ul> <p>It turns a monolithic model into something closer to a model <em>plus a growing library of plug-in skills</em>.</p> <h2 id="does-it-actually-work">Does it actually work?</h2> <p>We tested TokMem in two settings.</p> <p>The first is <strong>atomic recall</strong>: can a memory token reliably reproduce one specific procedure? We checked this across roughly a thousand tasks drawn from Super-Natural Instructions — a broad, messy mix of real instructions.</p> <p>The second is <strong>compositional recall</strong>: can the model chain several procedures together to solve a multi-step problem, the way you’d compose function calls?</p> <p>Two comparisons matter. Against <strong>retrieval-augmented prompting</strong> — the standard “look up the relevant instructions and paste them in” approach — TokMem comes out ahead <em>while shedding the repeated-context cost</em> that prompting pays on every query. And against <strong>parameter-efficient fine-tuning</strong>, it matches or exceeds the quality using substantially fewer trainable parameters.</p> <p>In other words, it’s competitive on accuracy and cheaper on both counts that usually trade off against each other.</p> <h2 id="why-i-think-this-matters">Why I think this matters</h2> <p>The mental model I keep coming back to: prompts treat a procedure as something you <em>re-explain</em>; TokMem treats it as something you <em>remember</em>. Once a behavior is worth keeping, it deserves a compact, reusable home — not a paragraph you re-type into the context window forever.</p> <p>If that idea scales, it points toward frozen models backed by ever-growing libraries of single-token skills: modular, cheap to extend, and free of the interference that makes continual learning so painful today.</p> <p>If you’d like the full details, the experiments, and the math I glossed over here, the paper is on <a href="https://arxiv.org/abs/2510.00444">arXiv</a>.</p>]]></content><author><name></name></author><category term="research"/><category term="llm"/><category term="memory"/><category term="nlp"/><category term="research"/><summary type="html"><![CDATA[Compiling each reusable task procedure into a single trainable token, with the language model kept frozen.]]></summary></entry><entry><title type="html">ULPT: tuning a soft prompt in two dimensions</title><link href="https://khalilbalaree.github.io/blog/2026/ulpt/" rel="alternate" type="text/html" title="ULPT: tuning a soft prompt in two dimensions"/><published>2026-06-15T00:00:00+00:00</published><updated>2026-06-15T00:00:00+00:00</updated><id>https://khalilbalaree.github.io/blog/2026/ulpt</id><content type="html" xml:base="https://khalilbalaree.github.io/blog/2026/ulpt/"><![CDATA[<p>Fine-tuning a modern language model is expensive. So a lot of recent work asks a cheaper question: instead of updating billions of weights, can we just learn a small “nudge” that steers a frozen model toward our task?</p> <p><strong>Prompt tuning</strong> is one of the tidiest answers. Rather than writing a prompt in words, you prepend a handful of <em>learned</em> vectors — “soft” prompt tokens — to the input and train only those. The model stays frozen; the soft prompt carries the task. It works surprisingly well, and you only store a tiny prompt per task instead of a whole model.</p> <p>But there’s a hidden tax, and that tax is what <a href="/publications/">ULPT</a> <a class="citation" href="#wu2025ulpt">(Wu et al., 2026)</a> goes after.</p> <h2 id="the-hidden-tax-the-model-picks-your-dimension">The hidden tax: the model picks your dimension</h2> <p>Here’s the catch with soft prompts. Each prompt vector has to live in the model’s <strong>hidden dimension</strong> — the width the model uses internally, often several thousand numbers wide. So even a “small” soft prompt of, say, 10 tokens isn’t 10 numbers; it’s 10 × <em>d</em>, where <em>d</em> might be 2048 or 4096.</p> <p>You didn’t choose that width. The model did. Your parameter budget is dictated by the architecture, not by how much steering your task actually needs — and that puts a floor on how cheap prompt tuning can get.</p> <div class="text-center"> <img src="/assets/img/blog/fig1-ulpt-params.svg" alt="Vanilla prompt tuning learns full-width prompt vectors; ULPT learns tiny low-dimensional vectors expanded by a frozen random matrix, for about 98% fewer trainable parameters." style="max-width:100%; height:auto;"/> <p class="post-description">Vanilla prompt tuning trains the full width; ULPT trains a tiny vector and expands it for free.</p> </div> <h2 id="the-idea-tune-small-project-up">The idea: tune small, project up</h2> <p>ULPT’s move is almost cheeky in its simplicity. <strong>Optimize the prompt in a tiny space — as few as two dimensions — and use a fixed random matrix to blow it back up to the width the model expects.</strong></p> <p>Concretely, for each soft-prompt position you learn a tiny vector <em>z</em> (think 2D). A <strong>frozen random matrix</strong> <em>R</em> up-projects <em>z</em> into the full <em>d</em>-dimensional embedding the model consumes. You only ever train the little <em>z</em> vectors. The random matrix is generated once and never updated — it costs nothing to learn and nothing to store (you can regenerate it from a seed).</p> <div class="text-center"> <img src="/assets/img/blog/fig2-ulpt-projection.svg" alt="A tiny trainable vector z is up-projected by a frozen random matrix into a full-width prompt embedding fed to a frozen language model; only z is learned." style="max-width:100%; height:auto;"/> <p class="post-description">Only the little vector is learned; the random projection and the model stay fixed.</p> </div> <h2 id="why-does-a-random-matrix-work">Why does a <em>random</em> matrix work?</h2> <p>This is the part that feels like it shouldn’t work, but does. A random projection doesn’t need to be “trained” to be useful — random high-dimensional maps tend to preserve the structure of whatever you feed through them. The fixed matrix acts like a rich, pre-laid set of directions in the model’s embedding space, and the handful of trainable coordinates in <em>z</em> just decide how to mix them.</p> <p>In other words, the expressive <em>width</em> still comes from the model’s full dimension — you simply don’t have to <em>pay</em> for it in trainable parameters. The learning happens in a two-dimensional knob box; the random matrix turns those knobs into a full-width signal.</p> <h2 id="does-it-actually-work">Does it actually work?</h2> <p>Yes, and by a comfortable margin on the metric that matters here. ULPT reaches roughly a <strong>98% reduction in trainable parameters</strong> compared to vanilla prompt tuning <em>while preserving performance</em>.</p> <p>And it’s not a one-task fluke. Across <strong>more than 20 NLP tasks</strong>, ULPT consistently outperforms recent parameter-efficient tuning methods while using significantly fewer parameters. That combination — competitive accuracy at a fraction of the parameter cost — is exactly what you want if you plan to host many task-specific customizations of the same backbone.</p> <h2 id="spend-your-budget-on-length-not-width">Spend your budget on length, not width</h2> <p>There’s a result here I find especially telling. Fix the parameter budget — say you’re allowed a certain number of trainable values, full stop — and ask how best to spend it. You could buy a <strong>short prompt of full-width vectors</strong>, or a <strong>long prompt of low-dimensional ones</strong>. ULPT shows the long, low-dimensional prompt wins, and not by a little.</p> <div class="text-center"> <img src="/assets/img/blog/fig3-ulpt-length-vs-width.svg" alt="With the same parameter budget, a short full-width prompt uses few high-dimensional positions while a long low-dimensional prompt uses many positions; the long low-dimensional prompt reaches higher task performance." style="max-width:100%; height:auto;"/> <p class="post-description">Same budget either way — but spending it on more low-dim positions beats fewer full-width ones.</p> </div> <p>It’s an intuitive trade once you say it out loud: with the same budget, would you rather have a few very precise steering vectors or many coarser ones? It turns out <em>more positions to steer from</em> matters more than <em>more precision per position</em>. The model benefits from a longer soft prompt it can attend to, and ULPT’s cheap-per-token design is what makes that length affordable in the first place. Width was the expensive luxury; length is where the budget is better spent.</p> <h2 id="why-i-think-this-matters">Why I think this matters</h2> <p>If you want to serve a model that behaves differently for hundreds or thousands of customers or tasks, the bottleneck isn’t really inference — it’s <strong>storage and management of all those adaptations</strong>. ULPT shrinks each adaptation to a few numbers per prompt position, which makes “a model plus a giant library of cheap task tweaks” genuinely practical.</p> <p>The deeper takeaway I keep coming back to: the dimensionality of <em>what you tune</em> doesn’t have to equal the dimensionality of <em>what the model uses</em>. A fixed random map cleanly decouples the two — and once you see that, a lot of parameter budget turns out to be optional.</p> <p>The full method, ablations, and per-task numbers are in the <a href="https://arxiv.org/abs/2502.04501">paper</a>.</p>]]></content><author><name></name></author><category term="research"/><category term="llm"/><category term="prompt-tuning"/><category term="efficiency"/><category term="nlp"/><category term="research"/><summary type="html"><![CDATA[Tuning prompts in an ultra-low-dimensional space and letting a frozen random matrix do the rest, for ~98% fewer trainable parameters.]]></summary></entry><entry><title type="html">Carrying a soft prompt from one model to another</title><link href="https://khalilbalaree.github.io/blog/2026/zero-shot-prompt-transfer/" rel="alternate" type="text/html" title="Carrying a soft prompt from one model to another"/><published>2026-06-14T00:00:00+00:00</published><updated>2026-06-14T00:00:00+00:00</updated><id>https://khalilbalaree.github.io/blog/2026/zero-shot-prompt-transfer</id><content type="html" xml:base="https://khalilbalaree.github.io/blog/2026/zero-shot-prompt-transfer/"><![CDATA[<p>Say you’ve spent real effort tuning a soft prompt — a little set of learned vectors that makes a model good at some task. Then a newer, better model comes out. Can you just hand your prompt over to the new model?</p> <p>With a <em>text</em> prompt, sure — words are words, and you can paste them anywhere. But a <strong>continuous</strong> (soft) prompt isn’t words. It’s a bundle of raw embedding vectors tuned to one specific model’s internal space. Hand those numbers to a different model and they mean nothing. The effort doesn’t transfer.</p> <p>That portability gap is what <a href="/publications/">zero-shot continuous prompt transfer</a> <a class="citation" href="#wu2024zeroshot">(Wu et al., 2024)</a> sets out to close.</p> <h2 id="why-soft-prompts-dont-travel">Why soft prompts don’t travel</h2> <p>Every model carries its own private map of meaning. Two models might both “understand” language, but the <em>coordinates</em> they use are completely different — the direction that means “about sports” in one model points somewhere unrelated in another.</p> <p>A soft prompt is just a point (well, a few points) on one model’s map. Copy those exact coordinates onto a different map and you land somewhere arbitrary. The prompt was never a statement about the task in the abstract; it was a statement in one model’s private dialect.</p> <div class="text-center"> <img src="/assets/img/blog/fig1-cpt-problem.svg" alt="A soft prompt tuned in Model A's embedding space lands on a meaningless location when its vectors are copied directly into Model B's differently-shaped space." style="max-width:100%; height:auto;"/> <p class="post-description">The same coordinates mean different things on different maps, so a direct copy fails.</p> </div> <h2 id="the-idea-describe-a-prompt-by-what-its-near">The idea: describe a prompt by what it’s near</h2> <p>The fix is to stop describing the prompt by its <em>absolute</em> coordinates and start describing it by its <em>relationships</em>. Pick a set of ordinary words that both models know — “river,” “happy,” “run,” and so on — and use them as <strong>anchors</strong>. Then describe the source prompt not as “these vectors,” but as “this much like <em>river</em>, this much like <em>happy</em>, this much like <em>run</em>…”</p> <p>That description — a list of similarities to shared anchor words — is the <strong>relative representation</strong>. And crucially, it’s <em>model-agnostic</em>: because both models know the same anchor words, both can speak in this common language. It captures the <strong>task semantics</strong> of the prompt while throwing away the model-specific coordinate frame.</p> <p>To transfer, you flip the process around on the target model: <strong>search for a prompt in the new model whose relationships to those same anchors match the source’s</strong>. No labeled data, no retuning on the target — just find the point on the new map that has the same description.</p> <div class="text-center"> <img src="/assets/img/blog/fig2-cpt-relative.svg" alt="The source prompt is encoded as similarities to shared anchor words, giving a model-agnostic relative representation, and a matching prompt is searched for in the target model." style="max-width:100%; height:auto;"/> <p class="post-description">Encode the prompt as relations to shared words, then find the prompt in the new model with the same relations.</p> </div> <h2 id="does-it-actually-work">Does it actually work?</h2> <p>It does. Prompts transferred this way carry their task ability across to other models <strong>without any tuning on the target side</strong> — confirming that the “task semantics” baked into a continuous prompt really are generalizable, not just an artifact of one model.</p> <p>There’s also a nice bonus. If you encode the <em>same</em> task from <strong>several source models</strong> and combine their relative representations, the transfer gets even better. Pooling several models’ takes on a task washes out each one’s idiosyncrasies and leaves a cleaner signal of what the task actually is.</p> <h2 id="why-i-think-this-matters">Why I think this matters</h2> <p>Soft prompts are cheap to store but, until now, awkwardly disposable — retire a model and the prompts you tuned for it retire with it. Treating a prompt’s meaning as a set of <em>relationships</em> rather than fixed coordinates makes that knowledge portable: you can move task know-how onto new and better models as they arrive, instead of starting over each time.</p> <p>The broader idea I like here is the reframing — <strong>meaning as relations, not coordinates.</strong> A point is only interpretable relative to a shared frame of reference, and once you choose anchors everyone agrees on, a surprising amount of “model-specific” stuff turns out to be translatable after all.</p> <p>The full method, the relative-space encoding, and the experiments are in the <a href="https://arxiv.org/abs/2310.01691">paper</a>.</p>]]></content><author><name></name></author><category term="research"/><category term="llm"/><category term="prompt-tuning"/><category term="transfer"/><category term="nlp"/><category term="research"/><summary type="html"><![CDATA[Describing a soft prompt by what it's near, so its task semantics carry across different language models.]]></summary></entry></feed>